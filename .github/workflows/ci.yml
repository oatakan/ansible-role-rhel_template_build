name: CI

on:
  pull_request:
  push:
    branches: [main, master]

env:
  ANSIBLE_ROLES_PATH: ${{ github.workspace }}/tests/roles

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install ansible ansible-lint yamllint

      - name: Install required collections
        run: |
          ansible-galaxy collection install community.general ansible.posix

      - name: Set up role locally
        run: |
          mkdir -p tests/roles
          ln -s ${{ github.workspace }} tests/roles/oatakan.rhel_template_build

      - name: Run yamllint
        run: yamllint .

      - name: Run ansible-lint
        run: ansible-lint

  syntax-check:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install Ansible
        run: |
          python -m pip install --upgrade pip
          pip install ansible

      - name: Set up role locally
        run: |
          mkdir -p tests/roles
          ln -s ${{ github.workspace }} tests/roles/oatakan.rhel_template_build

      - name: Install required collections
        run: |
          ansible-galaxy collection install community.general ansible.posix

      - name: Syntax check
        run: |
          ansible-playbook -i tests/inventory tests/test.yml --syntax-check

  test-docker:
    runs-on: ubuntu-latest
    needs: syntax-check
    strategy:
      matrix:
        distro:
          - rockylinux:8
          - rockylinux:9
          - almalinux:8
          - almalinux:9
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install 'ansible-core==2.14.*' molecule molecule-plugins[docker]
          pip install 'urllib3<2' 'requests<2.32'

      - name: Set up role locally
        run: |
          mkdir -p tests/roles
          ln -s ${{ github.workspace }} tests/roles/oatakan.rhel_template_build

      - name: Install collections
        run: |
          ansible-galaxy collection install community.general community.docker ansible.posix

      - name: Run Molecule tests
        run: |
          molecule test
        env:
          MOLECULE_DISTRO: ${{ matrix.distro }}

  # Optional: Test with Vagrant (requires self-hosted runner with virtualization)
  test-vagrant:
    runs-on: self-hosted  # You need to set up a self-hosted runner
    needs: syntax-check
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    strategy:
      matrix:
        box:
          - generic/rocky8
          - generic/rocky9
    steps:
      - uses: actions/checkout@v4

      - name: Set up role locally
        run: |
          mkdir -p tests/roles
          ln -s ${{ github.workspace }} tests/roles/oatakan.rhel_template_build

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install 'ansible-core==2.14.*' molecule molecule-plugins[docker]
          pip install 'urllib3<2' 'requests<2.32'

      - name: Run Molecule vagrant tests
        run: |
          cd molecule/vagrant
          molecule test
        env:
          MOLECULE_VAGRANT_BOX: ${{ matrix.box }}
